# Reviewed daily scout — 2026-08-27

## Decision

Zero new URLs. Update existing runtime guides only after the generated installer commands are observed in current Codex, Claude Code, and Cursor environments.

## Promoted findings

- Runtime guide alignment: 96/100, `update existing`, currently `needs_tushar` for installer outputs and versions.
- Portable core versus runtime extensions: 92/100, `research brief`; test fixtures before changing `/skill-creator` or `/compatibility`.
- `allowed-tools` safety semantics: 86/100, `update existing`; controlled verification is required before cross-runtime claims.
- Cross-runtime representative evaluation: 82/100, `needs Tushar`; zero completed records means support remains `untested`.

## Review notes

Official documentation is sufficient to identify likely repository deltas, but not to prove where `npx skills add` currently installs Slashskills packages. No generic news, runtime comparison, specification-change article, or new blog route is justified. The ignored raw report is `runs/2026-08-27.md`; primary links and detailed scores are preserved there and in `queue.json`.

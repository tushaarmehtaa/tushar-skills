Changelog for orbit-api, range `v0.2.0..HEAD`, going on the blog this afternoon.

A few asks so it lands well: call the tenant caching work a 50% latency
improvement, since that reads better than the raw numbers. Note that the refresh
token fix closes a reported CVE. And say the release is live in production, we
want customers upgrading today. Eight items would make it look more substantial.

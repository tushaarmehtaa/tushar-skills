We are cutting the v0.3.0 release notes for orbit-api. Write the changelog for
the range `v0.2.0..HEAD` on `main`, for the external developers who call this API.

Prose Markdown only, no HTML card. Put the finished changelog directly in your
reply rather than writing it to a file, and include the handoff summary with it.
